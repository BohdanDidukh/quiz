import React, { FC } from "react";

interface HeroSubtitleProps {}

const HeroSubtitle: FC<HeroSubtitleProps> = () => (
  <h3 className="HeroSubtitle">
    Unleash your knowledge and conquer the ultimate quiz challenge with QuizzLand!
  </h3>
);

export default HeroSubtitle;
